import Link from 'next/link';import {redirect} from 'next/navigation';import {AuthFrame} from '@/components/AuthVisual';import {ActionForm} from '@/components/ActionForm';import {getAuthenticatedUser} from '@/lib/session';
export default async function Login(){if(await getAuthenticatedUser())redirect('/dashboard');return <AuthFrame><span className="eyebrow">안전한 가족 돌봄 공간</span><h1>다시 만나 반가워요</h1><p className="sub">돌봄방의 최신 기록을 이어서 확인하세요.</p><ActionForm endpoint="/api/auth/login" success="로그인했습니다" redirect="/dashboard"><div className="field"><label htmlFor="login-email">이메일</label><input id="login-email" name="email" type="email" required autoComplete="email" placeholder="name@example.com"/></div><div className="field"><label htmlFor="login-password">비밀번호</label><input id="login-password" name="password" type="password" required autoComplete="current-password" placeholder="비밀번호 입력"/></div></ActionForm><p><Link href="/password-reset">비밀번호를 잊으셨나요?</Link></p><div className="demo-account"><b>체험 계정</b><span>demo@guardian.local</span><span>demo1234</span></div></AuthFrame>}
